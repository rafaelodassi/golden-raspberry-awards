import React from 'react';

import { render } from '@testing-library/react';

import StudiosWinCount from './StudiosWinCount';

describe('StudiosWinCount', () => {
  it('Render StudiosWinCount', async () => {
    render(<StudiosWinCount />);
  });
});
